import { ExibeListaPipe } from './exibe-lista.pipe';

describe('ExibeListaPipe', () => {
  it('create an instance', () => {
    const pipe = new ExibeListaPipe();
    expect(pipe).toBeTruthy();
  });
});
